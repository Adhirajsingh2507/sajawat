# SAJAWAT — OPEN TECHNICAL DEBT REGISTER

> Single source of truth for known, accepted debt and deferred work. Updated at
> every milestone. "Open" = not yet resolved. Resolved items move to the bottom.

- **As of commit (2026-06-15):** **Phase 1 in progress — entire backend commerce path built (1.1→1.6c).** Auth+sessions (1.2), catalog (categories/collections/products/inventory + search, 1.3), gated storefront foundation + catalog pages (1.4), cart/wishlist/promotions (1.5), orders + COD + Razorpay-online (config-gated) + admin order management (1.6). **134 API tests green.** `develop` carries 1.1→1.6a; **1.6b+1.6c committed locally on `feat/phase1.6-checkout` (HEAD `ed84d4d`), merging now.** Staging API auto-deploys on push to `develop` (API-only, AD-54). Phase-0 history below for reference.
- **Phase 0 baseline:** Milestone 0.10a complete (CI pipeline — GitHub Actions); canonical tree `ece7971` / `v0.10.0-phase0`. Phase 0: 0.1–0.9 + **0.10a done**. **0.10b CD now fully AUTHORED:** 0.10b.1 GCP provisioning scripts + 0.10b.2 `deploy-staging.yml` + **0.10b.3 `deploy-production.yml`** (tag `v*` trigger, required-reviewer `production` Environment, keyless prod WIF, staging→prod digest promotion (AD-56), automated post-shift rollback (AD-57)) — all statically validated (`actionlint`/`shellcheck`/YAML/`bash -n` clean). **D16 is implementation-complete; closure is PENDING OPERATOR ACTIVATION** — see D16. **Verified live state (GitHub API):** the `staging` Environment exists but has **0 variables / no protection**; the single `deploy-staging.yml` run **failed at the WIF auth step** (quality gate passed); the `production` Environment **does not exist yet**; no GCP provisioning has run. A **manual** `gcloud run deploy` staging service is live and verified (`https://sajawat-api-staging-1019894285252.asia-south1.run.app`), but it is ad-hoc, unversioned, and uses hand-injected secrets — it does **not** satisfy 0.10b. (`main` HEAD `bbf068d` is a post-0.10a administrative commit — only `.claude/settings.local.json`, no app code; kept in history.)

## Open Debt

| ID | Severity | Description | Planned Resolution | Milestone |
|----|----------|-------------|--------------------|-----------|
| D-SF1 | Low | **Storefront SKU videos** — PDP gallery video slot is built and verified (`PublicProduct.video`), but no real jewellery videos exist; `apps/web/public/demo` uses DEMO-ONLY stock imagery and no video is seeded. | Client to provide product photography + videos; then seed `video.url` and swap demo images. | Storefront redesign (2026-07) |
| D-SF2 | Low | **PDP offer price basis** — PLP `minPrice`/`maxPrice` and offer display use the **base list price**, not the discounted `salePrice` (deliberate, predictable). | Revisit if the client wants filtering/deals on effective (sale) price. | Storefront redesign (2026-07) |
| D-SF3 | Low | **Shared `Container` width** raised 1280→1600px widened `apps/admin` too (uses the same `@sajawat/ui` primitive). | Harmless (more space); scope to `apps/web` only if admin should stay narrow. | Storefront redesign (2026-07) |
| D-SF4 | Low | **Demo barcodes are placeholders** — `seed-demo.ts` assigns deterministic `8901…` demo barcodes so the scan-to-receive flow is exercisable; these are not the client's real printed codes. | Client to provide/print real barcodes; re-seed or edit via the product form. | Barcode (2026-07-05) |
| D-CI1 | Low | **CI "Security scan" red on every PR** — `dependency-review-action` needs Dependency Graph + GitHub Advanced Security, not enabled on this private repo; unrelated to any diff. | Enable GHAS, or make the dependency-review step non-blocking on private repos. | Ops |
| D3 | Low | Local pnpm installed via npm user-prefix, not Corepack (Corepack's shim is broken on Node 25). Local provisioning diverges from the documented Corepack-on-Node-22 path. **0.8/0.10a: the Docker build and CI both use the documented Corepack-on-Node-22 path (validated).** | Documented; CI/Docker use Corepack on Node 22. No code change needed. | — (doc-only) |
| ~~D4~~ | Low | ~~Next.js starter boilerplate present in both apps.~~ **RESOLVED** — `apps/web` starter replaced across 1.4a/1.4b/1.4c (storefront), `apps/admin` starter replaced across 1.7a/1.7b (operations console). Both apps ship real product UI. | Done. | 1.4 / 1.7 |
| D5 | Low | `.prettierignore` excludes **all** `**/*.md` (protects hand-formatted specs but means Markdown is never format-enforced). | Optionally narrow to `docs/` + root specs so other Markdown stays formatted. | optional |
| D7 | Low | `@sajawat/config` is lint-exempt (echo stub) — config-only package, no lintable TS sources. | Accepted exemption; revisit only if TS is added there. | — (accepted) |
| D10 | Low | `services/api` `dist/` build output exists locally but is git-ignored; the API is consumed only by its own runtime (not by another workspace), so no project-reference/build-order coupling yet. | None needed; revisit if another workspace imports `@sajawat/api`. | — (accepted) |
| ~~D12~~ | Low | ~~helmet CSP disabled; the Next apps ship no per-app CSP.~~ **RESOLVED (1.9a)** — `apps/web` + `apps/admin` now emit a strict **nonce-based CSP** (`script-src 'self' 'nonce-…' 'strict-dynamic'`, no `'unsafe-inline'` for scripts) plus hardening headers (X-Content-Type-Options, Referrer-Policy, X-Frame-Options DENY, Permissions-Policy, prod HSTS + upgrade-insecure-requests) via the Next 16 `proxy` convention; root layouts opt into dynamic rendering so the per-request nonce stamps. The API stays CSP-off by design (JSON). | Done. | 1.9a |
| D14 | Low | The readiness endpoint returning **503** while the DB is down is logged at **error** level by pino-http's 5xx→error mapping. Under a sustained outage, frequent load-balancer probes of `/api/v1/health` will emit repeated error logs. (Liveness `/health` is unaffected — it stays 200.) | Optionally downgrade readiness-probe log level, or rate-limit/skip logging for the readiness route, when log noise becomes a concern. | optional |
| D17 | Medium | **Fully login-gated storefront (owner decision, AD-1.4G).** Per explicit product-owner choice, the entire storefront requires registration/login before any browsing (1.4a gate). **Accepted trade-offs, surfaced and confirmed:** (1) **SEO is sacrificed** — crawlers can't log in, so the PRD's "SEO First" / organic-traffic mandate, sitemap, structured-data, and crawlable metadata are **moot** (1.4b SEO scope dropped); (2) **top-of-funnel conversion barrier** — visitors meet a login wall, not the brand; (3) the catalog API itself stays public (the gate is a frontend UX boundary; API-level gating of reads is NOT implemented). | Revisit if organic growth becomes a priority: switch to public-browse + login-to-buy (the recommended model) — the auth UI and catalog already support it; only the gate placement changes. Until then, accepted. | 1.4 |
| D18 | Medium | **Razorpay online payments are DORMANT (config-gated).** `RazorpayProvider` + checkout/verify/webhook are fully implemented and tested with mocked HMAC, but `RAZORPAY_*` keys are absent → online checkout returns **501** and the webhook can't verify real events. COD is fully functional without keys. | Inject live `RAZORPAY_KEY_ID`/`KEY_SECRET`/`WEBHOOK_SECRET` into Secret Manager (staging then prod) and register the webhook URL in the Razorpay dashboard; then run an end-to-end live payment + webhook drill before relying on it. | 1.6 / 1.0-OH |
| D19 | Low | **WhatsApp B2B lead alerts are DORMANT (config-gated).** `NotificationProvider` + `WhatsAppProvider` (Meta Cloud API) are implemented; with `WHATSAPP_*` keys absent (or no admin number in `/admin/settings`) the alert is **skipped and logged** — the lead still persists. The live send path is untested end-to-end (no keys); only the dormant-skip branch is covered. Notification is awaited best-effort, so a slow provider slightly delays the enquiry response. | Inject `WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_ACCESS_TOKEN` into Secret Manager + set the admin number in settings; run a live lead-alert drill. Consider moving the send off the request path (queue) in Phase 2. | 1.8 / 1.0-OH |
| ~~D20~~ | Low | ~~**`trust proxy: true` is XFF-spoofable (rate-limit bypass).** `app.ts` sets `app.set('trust proxy', true)`, which trusts the **entire** `X-Forwarded-For` chain, so a client can spoof an XFF entry to land in a fresh per-IP rate-limit bucket and evade `globalRateLimiter`/`authRateLimiter`/the enquiry limiter.~~ **RESOLVED (1.0-OH)** — `trust proxy` is no longer a blanket `true`. It is now driven by a new `TRUST_PROXY` env var, parsed/defaulted in `config/env.ts` (`trustProxy` export) and applied in `app.ts`: dev/test default to `false` (req.ip = socket, no proxy locally), and **deployed environments default to a pinned single hop (`1`)** for Cloud Run. Accepts `false` / integer hop count / Express preset / CIDR list; `true` is still expressible but discouraged. Covered by `env.trust-proxy.test.ts` (9 cases). **Operator step:** set `TRUST_PROXY=2` once a *proxied* Cloudflare is confirmed in front of Cloud Run in staging (the one topology-dependent value the code can't auto-detect). | Done. | 1.0-OH |
| D21 | Low | **Stateful account lockout not implemented.** Brute-force defense is per-IP rate limiting only (`authRateLimiter`, 10/15min). There is no per-account lockout after N failed logins (PRD/security-design call for it), so a distributed/low-rate credential-stuffing attack on a single account isn't specifically slowed. | Add a stateful lockout (counter + temporary lock) — needs the Phase-2 Redis/session store for the counter with TTL; track alongside D15's rotation store. | Phase 2 |
| D22 | Low | **Inventory integrity is atomic, not transactional.** No-oversell is guaranteed by atomic conditional `$inc` per line item (works on Atlas + in-memory test server, which has no replica-set transactions). A multi-item order is therefore a sequence of atomic ops, not one ACID transaction — a mid-loop crash could leave a partial reservation. Mitigated by reserve-on-create + `InventoryMovement` audit trail + cancel/restock release path. | Wrap order placement in a Mongo multi-doc transaction (requires replica set) if partial-reservation incidents appear; revisit at scale. | Phase 2 |
| ~~D23~~ | Low | ~~**Web shopping UI is incomplete.** The storefront has auth + gated catalog/PLP/PDP/search (1.4), but cart, checkout, order-history, and account pages are not yet built — the full commerce path exists only at the API layer. Admin panel UI (1.7) and B2B CRM + WhatsApp alert (1.8) are likewise pending.~~ **RESOLVED** — storefront shopping UI shipped in **1.4c** (cart, wishlist, checkout, order-history, account), admin operations console in **1.7a/1.7b**, and B2B wholesale enquiry → CRM lead + WhatsApp alert in **1.8**. The full B2C commerce path now exists end-to-end in `apps/web`; admin UI in `apps/admin`. (Razorpay/WhatsApp live-key activation remains tracked separately as D18/D19.) | Done. | 1.4c / 1.7 / 1.8 |
| D16 | Medium | **Automated CD — STAGING ACTIVATED & VERIFIED ✅ (2026-06-10); PRODUCTION authored-but-unrun.** All three sub-milestones written + statically validated; **staging now proven live**: `sajawat-api-staging` last deployed by `sajawat-deployer@sajawat-staging.iam.gserviceaccount.com` (WIF deployer SA, not human), by immutable digest (`api@sha256:…`), serving revision healthy + Atlas connected — so GCP provisioning, WIF, Artifact Registry, Secret Manager, deployer+runtime SAs, and the `staging` Environment all work. **Production NOT yet exercised:** `sajawat-production` has **0 Cloud Run services**; the tag-`v*` promotion pipeline + automated rollback (AD-57) is unproven. | **D16 CLOSURE CRITERIA:** (1) ✅ provisioning green on **staging** (prod TBD); (2) staging secrets injected ✅ (prod TBD; rotation = 1.0-OH); (3) prod deployer SA granted `artifactregistry.reader` on the **staging** repo (AD-56) — TBD; (4) ✅ `staging` Environment variables populated; (5) `production` Environment with **required reviewer** + variables — TBD; (6) ✅ **successful staging deploy** (promotable digest exists); (7) tag `v*` → **one successful gated production deploy** — TBD; (8) **one rollback drill** proving AD-57 — TBD. Staging side done; **close D16 when (3)(5)(7)(8) pass on production.** | 0.10b |

## Resolved Debt (history)

| ID | Resolved in | Description | How resolved |
|----|-------------|-------------|--------------|
| D15 | 1.2 | Stateless refresh — no server-side revocation/rotation. | Persisted `sessions` collection (one row per refresh `jti`, TTL on `expiresAt`); `SessionService.rotate` revokes the old `jti` on every refresh and **revokes the whole `family` on replay of a rotated token** (reuse-detection); logout revokes the active session; `refresh` re-checks the live user. Covered by service + integration tests. |
| R1 | 0.2 | Cross-package consumption strategy was unsolved (raw `./src/index.ts` main, no transpile/build plan). | AD-1 role-based strategy: `shared` compiled, `ui`/`types` source + `transpilePackages`. |
| R2 | 0.2 | "Fake-green" typecheck — 5 packages echoed `exit 0`. | Real `tsc` in all TS packages; project references. |
| — | 0.2 | Missing Husky pre-commit gate. | Husky v9 `pre-commit` + `commit-msg`. |
| — | 0.2 | Missing root `README.md`. | Comprehensive root README created. |
| — | 0.2 | Missing mandated top-level dirs (`scripts/`, `tests/`, `infrastructure/`). | Created with documented sub-structure. |
| — | 0.2 | Five Turbo "no output files" warnings. | Removed build stubs from non-emitting packages. |
| D2 | 0.3 (partial) | lint-staged ESLint gap (couldn't resolve root eslint) + per-package "lint configured in 0.3" stubs. | Single root flat config + root `eslint` devDep; ESLint re-enabled in lint-staged with `--no-warn-ignored`. (Residual stubs tracked as D8.) |
| D6 | 0.4 | Non-type-aware ESLint — type-aware rules (`no-floating-promises`, `no-misused-promises`) were off, relevant for the async-heavy API. | Added `@sajawat/config/eslint/type-checked` (`recommendedTypeChecked` + the two promise rules) scoped to `services/api/**` via `projectService` (no per-config `parserOptions.project` needed). Refined the Express/Mongoose CJS-interop selector to exclude type-only imports so `import type { Request }` is allowed. |
| D11 | 0.7 | No CSRF protection (security-design mandates it). | **AD-17 hybrid transport** makes the API surface CSRF-immune (Bearer access token — browsers don't auto-attach it); the refresh cookie is httpOnly + SameSite=Strict, and a **double-submit CSRF guard** (`middleware/csrf.ts`, AD-22) protects the cookie-authenticated endpoints (refresh/logout, mounted Phase 1). |
| D8 | 0.9 | Placeholder `echo … exit 0` `test` scripts in api/web/admin. | Replaced with real `vitest run` across all three (63 tests). Only the accepted `@sajawat/config` lint/typecheck stubs remain — those are config-only and tracked as D7. |
| D1 | 0.10a | `engine-strict=false` left the Node 22 contract advisory in CI. | CI pins Node 22 via `actions/setup-node` (`node-version-file: .nvmrc`) **plus an explicit guard step** that fails the run if the major version ≠ 22. `engine-strict` intentionally **stays `false`** in `.npmrc` (local installs still run on Node 25); enforcement is a CI **runtime pin**, not a strict toggle. |
| D9 | 0.10a | No git tags / release versioning. | Adopted annotated `vX.Y.0-phase0` tagging per milestone. Retroactively tagged the previously-untagged milestones (0.4.1, 0.5, 0.6, 0.7, 0.8, 0.9) at their recorded commits and added `v0.10.0-phase0`. First production release `v1.0.0` lands at the end of Phase 1. |
| D13 | 0.10a | CI-side: required env + `MONGOMS_VERSION` for the CI test job. | The CI gate jobs need **no real secrets**: `test/setup.ts` self-provisions test JWT secrets + a placeholder `MONGODB_URI`; the workflow only pins `MONGOMS_VERSION=6.0.14` (env), which also keys the cached `mongodb-binaries`. App secrets appear **only** in the deferred 0.10b deploy jobs (Secret Manager). |

## Notes on Issues Found & Fixed In-Flight (not carried as debt)

- **0.3:** Next 16 removed the `eslint` NextConfig key and build-time linting → removed the invalid `ignoreDuringBuilds` addition.
- **0.3:** Flat-config emits a "File ignored" warning when lint-staged passes ignored config files → added `--no-warn-ignored`.
- **0.4:** `pino-http` default import is not callable under NodeNext → switched to the named import `import { pinoHttp } from 'pino-http'`.
- **0.4:** `tsc` could not name the inferred `Router` type portably (TS2742) → added an explicit `Router` type annotation (`import type { Router } from 'express'`).
- **0.4:** body-parser errors (malformed JSON, oversized body) initially surfaced as masked `500`s → the global handler now maps exposed `http-errors` 4xx (`expose === true`) to the correct status/code (verified: `400 BAD_REQUEST`, `413 PAYLOAD_TOO_LARGE`).
- **0.4.1:** helmet/cors/rate-limit were missing from the 0.4 scope and were not initially recorded as deferred (reporting gap). Closed by the 0.4.1 hardening patch; residual security work (CSP, CSRF) is now explicitly tracked as D11/D12. The `globalRateLimiter` skip list covers both `/health` and `/api/v1/health` so probes are never throttled.
- **0.5:** `mongoose` resolved to **`^9.6.3`** (the 0.5 plan anticipated `^8`; an unpinned `pnpm add` pulled the current major). Mongoose 9 bundles MongoDB driver `~7.2`, `engines.node >=20.19.0`. Mongoose 9 types `connection.readyState` as the `ConnectionStates` enum, which tripped `@typescript-eslint/no-unsafe-enum-comparison` on a `=== 0` literal compare → fixed by comparing against `mongoose.ConnectionStates.disconnected`. **Reviewed and APPROVED as the baseline** (greenfield, no models/repository yet; gates green; live smoke passed; Atlas + Node 22 compatibility acceptable) — **not** carried as debt; no downgrade.
- **0.6:** Per-environment configuration strategy completed (Node-native layered `--env-file`, per-env + per-app templates, AD-14 production guards, R-2 pre-commit secret guard, Environment Guide). No new dependencies. The Next.js apps' scaffolded `.gitignore` blanket-ignored `.env*` (incl. `.env.example`) → added `!.env.example` negation in `apps/web` + `apps/admin` so per-app templates are trackable while real env files stay ignored.
- **0.7:** Auth foundation (jose JWT utils, `@node-rs/argon2` password utils, centralized RBAC in `@sajawat/shared`, auth/CSRF middleware, auth rate limiter). `@node-rs/argon2` exports `Algorithm` as an **ambient const enum**, which `verbatimModuleSyntax` forbids referencing → dropped the explicit `algorithm` option and rely on the library's argon2id default (cost params set explicitly). Resolves **D11**; adds **D15** (stateless refresh — no server-side revocation until the Phase-1 session store).
- **0.8:** Docker & local orchestration (3 multi-stage images, compose with `mongo:7`). No runtime deps. **Caught in verification:** the compose API must run `NODE_ENV=staging` (not `development`) — dev mode loads `pino-pretty`, a **devDependency excluded from the `--prod` deploy image**, which would crash boot; `staging` also skips the AD-14 production localhost guard. The Next standalone server needs `HOSTNAME=0.0.0.0` to be reachable. Frontend `NEXT_PUBLIC_*` are build-time → images are environment-specific.
- **0.10a:** CI pipeline (`.github/workflows/ci.yml`, GitHub Actions). No deps. Three jobs: `quality` (Corepack pnpm → Node-22 guard → frozen install → `turbo run lint typecheck build` then `turbo run test -- --coverage` → coverage artifacts), `e2e` (cached chromium Playwright smoke), `docker` (build-validates all three 0.8 images on pushes, GHA layer cache, no push). Caching: pnpm store (setup-node), Turbo (`.turbo`), `mongodb-binaries` (keyed on `MONGOMS_VERSION`), Playwright browsers. **Ordering note:** `corepack enable` must precede `actions/setup-node` so its `cache: pnpm` resolves pnpm. Resolves **D1** (Node-22 runtime pin + guard), **D9** (tagging), **CI-side D13** (no secrets in gates; `MONGOMS_VERSION` pinned). Adds **D16** (CD deferred to 0.10b). **CD is intentionally absent.**
- **0.10b.2 (CD staging deploy workflow — authored, API-only):** Added `.github/workflows/_quality.yml` (reusable gate extracted from 0.10a `ci.yml`, shared by CI + deploy so they can't drift) and `deploy-staging.yml` (`push:develop` + dispatch; keyless WIF with `environment: staging`; build+push SHA-tagged API image to Artifact Registry; deploy **by digest** with `--no-traffic --tag=candidate` → `/api/v1/health` readiness gate → traffic shift; secrets via `--set-secrets`, `JWT_ISSUER`/`JWT_AUDIENCE` via `--set-env-vars`; `api-run` SA). **Scoped API-only (AD-54):** `apps/web`/`apps/admin` are unmodified `create-next-app` starter pages (**D4**) with no product UI and no Cloud Run services — deploying them now would be placeholder complexity, so frontend deploy automation defers to Phase 1.4 (web) / 1.7 (admin). **Validation:** `actionlint 1.7.7` + embedded `shellcheck` + Python YAML parse + `bash -n` all clean. **Not yet activated** (needs 0.10b.1 run, secret values injected, `staging` Environment variables). **Branch hygiene:** `develop` re-synced to `main` (`eeb6c00`) so its trigger reflects current code. **D16 remains open** (production CD + rollback is 0.10b.3).
- **0.10b.1 (CD infrastructure automation — in progress):** Authored idempotent `gcloud` provisioning scripts under `infrastructure/scripts/gcp/` (`lib.sh`, per-env `config.*.sh`, `00`–`05`, `provision.sh`, `verify.sh`, `README.md`). Two separate projects (AD-47); keyless **WIF** repo-pinned + bound on the GitHub Environment claim (AD-48/AD-50); per-service runtime SAs with only `api-run` reading secrets + least-priv deployer SA (AD-51); Artifact Registry; **empty** Secret Manager containers with stdin-only value injection (AD-52); idempotent create/bind-only gcloud (AD-53). **Static validation:** `bash -n` + `shellcheck -x 0.10.0` clean on all 11 files. **Not yet run against GCP** (operator-executed) and **no GitHub Actions deploy workflows** authored — those are 0.10b.2/0.10b.3. **D16 remains open.** Claude cannot authenticate to GCP, so provisioning is operator-run by design.
- **Post-0.10a (manual staging deploy — NOT a milestone):** A live Cloud Run staging service was stood up via an **ad-hoc `gcloud run deploy`** of the `ece7971` 0.8 API image (region `asia-south1`, project `1019894285252`). `/health`, `/api/v1/health`, and Atlas connectivity verified green. **This is explicitly not milestone 0.10b** — no deploy workflows, WIF, Artifact Registry, Secret Manager, or rollback exist in-repo, and the service's secrets were injected by hand. Recorded so the live URL is not mistaken for finished CD; **D16 stays open** (bumped Low→Medium given the unmanaged production-adjacent secrets). Secret rotation/verification is tracked as the **non-blocking** Phase-1.0 Operational Hardening (1.0-OH) task, not a Phase-1 gate. Separately, `main` HEAD `bbf068d` ("feat(api): add auth middleware") is a **post-0.10a administrative commit** — it ships no application code (only `.claude/settings.local.json`) and is untagged; **kept in history (no rewrite)**. Canonical Phase-0 tree is `ece7971` / `v0.10.0-phase0`.
- **0.9:** Testing foundation (Vitest/Supertest/memory-server/Playwright; 63 tests). No runtime deps. **Caught in verification:** (1) `mongodb-memory-server`'s default mongod **8.2.6 (ubuntu2404) SIGSEGVs** on this host (Docker `mongo:7` is fine) → pinned `MONGOMS_VERSION=6.0.14` in `test/setup.ts` (overridable in CI). (2) The type-aware ESLint layer fought test ergonomics (Supertest `res.body` is `any`; `expect(res.cookie)` reads an unbound method) → relaxed `no-unsafe-*` + `unbound-method` for `services/api/**/*.test.ts` + `test/**`. (3) NodeNext `.js`→`.ts` test resolution solved by a Vite resolver plugin (`@sajawat/config/vitest/base.mts`). (4) AD-36 tsconfig split (`tsconfig.build.json` emits `src`; `tsconfig.json` covers tests for the lint `projectService`). (5) `next start` warns under `output:'standalone'` — the e2e webServer still works; the standalone-correct path is the compose-targeted `PLAYWRIGHT_BASE_URL` mode.
