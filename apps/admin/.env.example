# ============================================================
# SAJAWAT — ADMIN PANEL ENV TEMPLATE  (apps/admin, Next.js :3001)
# ============================================================
# Loaded by the Next.js runtime (NOT Node `--env-file`). Next reads, in order:
#   .env  ->  .env.<NODE_ENV>  ->  .env.local  (see sajawat-environment-guide.md)
# Copy to `.env.local` for local development.
#
# Only NON-SECRET values may use the NEXT_PUBLIC_ prefix — these ship to the
# browser. Admin analytics are intentionally omitted (internal tool).
# ============================================================

# Public base URL of the API (versioned).
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1
