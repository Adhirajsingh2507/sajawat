/**
 * @sajawat/api — Express + TypeScript backend entry point.
 * The Controller -> Service -> Repository architecture, modules, middleware,
 * database connection, and /api/v1/health endpoint are implemented in
 * Milestones 0.4 and 0.5.
 */
export {};
